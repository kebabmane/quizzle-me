<%= simple_form_for(@fotd) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :fact_id %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
